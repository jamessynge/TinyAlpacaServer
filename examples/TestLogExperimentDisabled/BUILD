# Test of the TAS_DLOG and TAS_DCHECK macros when DISABLED.

cc_binary(
    name = "TestLogExperimentDisabled",
    srcs = ["TestLogExperimentDisabled.ino.cc"],
    deps = [
        ":TinyAlpacaServer",
        ":arduino_main",
    ],
)
